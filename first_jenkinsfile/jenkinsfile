pipeline {
    agent any

    stages {
        stage('Create Folder') {
            steps {
                // Directly create the directory using the mkdir command
                sh 'mkdir my-new-folder'
                echo "Directory 'my-new-folder' created."
            }
        }
    }
}
